using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class LevelHearts : MonoBehaviour
{
    public Image[] hearts;
    PlayerController playerHealthHeart;

    // Start is called before the first frame update
    private void Awake()
    {
        //playerHealthHeart.playerHealth = hearts.Length;
        
       
    }
    void Start()
    { 
        
        
           
    }

    // Update is called once per frame
    void Update()
    {
      //  Debug.Log("Player health in level heart scipt is ");
       // Debug.Log(playerHealthHeart.playerHealth);
        
    }
}
